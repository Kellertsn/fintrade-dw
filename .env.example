# ── Alpha Vantage ─────────────────────────────
# Get your free API key at: https://www.alphavantage.co/support/#api-key
ALPHA_VANTAGE_API_KEY=your_api_key_here

# ── AWS / LocalStack ──────────────────────────
# For local development, leave these as-is (LocalStack uses "test" credentials).
# For real AWS: remove AWS_ENDPOINT_URL from docker-compose.yml and set real values below.
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET=fintrade-raw

# ── PostgreSQL ────────────────────────────────
# Matches the values in docker-compose.yml — no changes needed for local dev.
POSTGRES_USER=fintrade
POSTGRES_PASSWORD=fintrade123
POSTGRES_DB=fintrade
